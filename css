:root {
  --bg: #08111f;
  --panel: #101c2e;
  --panel2: #14243a;
  --text: #e6edf7;
  --muted: #8da2bd;
  --line: #253a55;
  --accent: #47a3ff;
  --green: #25d366;
  --yellow: #ffd166;
  --red: #ff5c7a;
}

[data-theme="light"] {
  --bg: #f5f7fb;
  --panel: #ffffff;
  --panel2: #eef3fa;
  --text: #0f172a;
  --muted: #5b677a;
  --line: #d8e0eb;
  --accent: #0969da;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  background: radial-gradient(circle at top left, #16395f, var(--bg) 42%);
  color: var(--text);
  font-family: Inter, Segoe UI, Arial, sans-serif;
}

.topbar {
  position: sticky;
  top: 0;
  z-index: 10;
  backdrop-filter: blur(18px);
  background: color-mix(in srgb, var(--bg) 88%, transparent);
  border-bottom: 1px solid var(--line);
  padding: 22px 34px;
  display: flex;
  justify-content: space-between;
  gap: 24px;
  align-items: center;
}

.eyebrow {
  color: var(--accent);
  font-size: 12px;
  text-transform: uppercase;
  letter-spacing: 0.16em;
  font-weight: 800;
}

h1, h2, h3, p {
  margin-top: 0;
}

h1 {
  margin-bottom: 8px;
  font-size: clamp(28px, 4vw, 46px);
}

p {
  color: var(--muted);
  line-height: 1.5;
}

.actions {
  display: flex;
  gap: 10px;
}

button, select, input {
  background: var(--panel2);
  color: var(--text);
  border: 1px solid var(--line);
  border-radius: 12px;
  padding: 10px 14px;
  font-weight: 700;
}

button {
  cursor: pointer;
}

button:hover {
  border-color: var(--accent);
}

main {
  max-width: 1420px;
  margin: 0 auto;
  padding: 28px;
}

.brief-card,
.correlation-card,
.card {
  background: linear-gradient(180deg, var(--panel), color-mix(in srgb, var(--panel), black 8%));
  border: 1px solid var(--line);
  border-radius: 24px;
  padding: 24px;
  box-shadow: 0 18px 50px rgba(0,0,0,0.24);
}

.brief-card {
  margin-bottom: 20px;
}

.correlation-card {
  margin-bottom: 22px;
}

.correlation-grid {
  display: grid;
  grid-template-columns: 1fr 1fr 140px;
  gap: 12px;
  margin-bottom: 18px;
}

.correlation-result {
  margin-top: 14px;
  color: var(--muted);
  font-weight: 700;
}

.tabs {
  display: flex;
  gap: 10px;
  margin: 22px 0;
  overflow-x: auto;
}

.tab.active {
  background: var(--accent);
  color: white;
  border-color: var(--accent);
}

.tab-panel {
  display: none;
}

.tab-panel.active {
  display: block;
}

.grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 22px;
}

.wide {
  grid-column: 1 / -1;
}

.section-head {
  display: flex;
  justify-content: space-between;
  gap: 18px;
  align-items: flex-start;
  margin-bottom: 18px;
}

.live,
.snapshot,
.ai {
  display: inline-flex;
  padding: 5px 9px;
  border-radius: 999px;
  font-size: 11px;
  font-weight: 900;
  letter-spacing: .06em;
  margin-bottom: 10px;
}

.live {
  background: rgba(37, 211, 102, .15);
  color: var(--green);
}

.snapshot {
  background: rgba(255, 209, 102, .15);
  color: var(--yellow);
}

.ai {
  background: rgba(71, 163, 255, .15);
  color: var(--accent);
}

.metric-row {
  display: grid;
  grid-template-columns: 1.2fr .8fr .6fr;
  gap: 12px;
  align-items: center;
  padding: 12px 0;
  border-bottom: 1px solid var(--line);
}

.metric-row:last-child {
  border-bottom: 0;
}

.metric-name {
  font-weight: 800;
}

.metric-value {
  color: var(--muted);
}

.pill {
  justify-self: end;
  border-radius: 999px;
  padding: 6px 10px;
  font-size: 13px;
  font-weight: 900;
}

.up {
  background: rgba(255, 92, 122, .14);
  color: var(--red);
}

.down {
  background: rgba(37, 211, 102, .14);
  color: var(--green);
}

.neutral {
  background: rgba(141, 162, 189, .18);
  color: var(--muted);
}

input {
  width: 100%;
  margin-bottom: 12px;
}

table {
  width: 100%;
  border-collapse: collapse;
  overflow: hidden;
  border-radius: 16px;
}

th, td {
  text-align: left;
  padding: 12px;
  border-bottom: 1px solid var(--line);
}

th {
  color: var(--muted);
  font-size: 12px;
  text-transform: uppercase;
  letter-spacing: .08em;
}

dialog {
  max-width: 620px;
  border: 1px solid var(--line);
  border-radius: 24px;
  background: var(--panel);
  color: var(--text);
  padding: 28px;
}

@media (max-width: 850px) {
  .topbar,
  .section-head {
    flex-direction: column;
  }

  .grid,
  .correlation-grid {
    grid-template-columns: 1fr;
  }

  main {
    padding: 18px;
  }
}
